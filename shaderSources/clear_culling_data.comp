#version 460

#include "shared.inc.glsl"

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0, scalar) buffer CullingData {
    FrustumCullingData frustums[];
} cullingData;

void main() {
    // cullingData.frustums[gl_GlobalInvocationID.x].shadedOpaqueMapModelsOutputDrawInfo.count = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].unshadedOpaqueMapModelsOutputDrawInfo.count = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].shadedMapModelsOutputDrawInfo.count = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].unshadedMapModelsOutputDrawInfo.count = 0;

    // // TODO: Initialize actor wall draw infos so that only the instance count needs to be reset
    // cullingData.frustums[gl_GlobalInvocationID.x].shadedActorWallsDrawInfo.vertexCount = 12;
    // cullingData.frustums[gl_GlobalInvocationID.x].shadedActorWallsDrawInfo.instanceCount = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].shadedActorWallsDrawInfo.firstVertex = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].shadedActorWallsDrawInfo.firstInstance = 0;

    // cullingData.frustums[gl_GlobalInvocationID.x].unshadedActorWallsDrawInfo.vertexCount = 12;
    // cullingData.frustums[gl_GlobalInvocationID.x].unshadedActorWallsDrawInfo.instanceCount = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].unshadedActorWallsDrawInfo.firstVertex = 0;
    // cullingData.frustums[gl_GlobalInvocationID.x].unshadedActorWallsDrawInfo.firstInstance = 0;

    // TODO: Move actor wall buffer clearing to here instead of on the CPU
}
