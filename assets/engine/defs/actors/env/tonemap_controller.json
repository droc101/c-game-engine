{
  "description": "Controls HDR tonemapping",
  "extends": "actor",
  "display": {
    "directional": false
  },
  "inputs": {
    "set": {
      "type": "none",
      "description": "Set the map's tonemapping information from this actor"
    },
    "set_instant": {
      "type": "none",
      "description": "Immediately set the map's tonemapping information from this actor without interpolation"
    }
  },
  "outputs": {},
  "params": {
    "exposure": {
      "display": "Exposure",
      "default": 1.0,
      "type": "float",
      "minimum": 0.0
    },
    "start_on": {
      "display": "Start On",
      "default": true,
      "type": "bool",
      "description": "Whether this light should be enabled when the map loads"
    },
    "interpolation_ticks": {
      "display": "Interpolation Ticks",
      "default": 60,
      "type": "int",
      "min": 0,
      "description": "The number of physics ticks (60 per second) the light color and angle transition should take"
    }
  }
}