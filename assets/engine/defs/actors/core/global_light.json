{
  "description": "Directional light",
  "extends": "actor",
  "display": {
    "color_param": "light_color"
  },
  "inputs": {
    "set": {
      "type": "none",
      "description": "Set the map's light information from this actor"
    },
    "set_instant": {
      "type": "none",
      "description": "Immediately set the map's light information from this actor without interpolation"
    }
  },
  "outputs": {},
  "params": {
    "light_color": {
      "display": "Light Color",
      "default": -1,
      "type": "color",
      "showAlpha": false,
      "description": "The color of this light"
    },
    "start_on": {
      "display": "Start On",
      "default": true,
      "type": "bool",
      "description": "Whether this light should be enabled when the map loads"
    },
    "interpolation_ticks": {
      "display": "Interpolation Ticks",
      "default": 60,
      "type": "int",
      "min": 0,
      "description": "The number of physics ticks (60 per second) the light color and angle transition should take"
    }
  }
}